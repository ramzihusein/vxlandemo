---
- name: backup nxos running config
  hosts: all
  gather_facts: false

  tasks:

    - name: Backup nxos config
      cisco.nxos.nxos_config:
        backup: true
        backup_options:
          filename: "backup_{{ ansible_hostname }}.cfg"
          dir_path: /home/rhusein/backups